% African Computer Modern Roman 
if unknown cmbase: input cmbase fi

font_identifier:="FCR"; font_size design_size*pt#;

univ_acc_breadth:=.5;  % zusatz parameter for the universal accent
input c-cmr;           % compute size parameters for CMR family

generate fcroman       % switch to the driver file
