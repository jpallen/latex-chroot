% African Computer Modern Unslanted Text Italic
if unknown cmbase: input cmbase fi

font_identifier:="FCU"; font_size design_size * pt#;

univ_acc_breadth:=.5;  % zusatz parameter for the universal accent
input c-cmti;          % compute cm text italic parameters
slant:=0;

generate fcitalic      % switch to the driver file
