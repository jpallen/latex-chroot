% This is an empty MetaPost source file. Why do you want it? Because if
% mp says `Please type another input file name:' and if you don't have one
% in mind, you can just say `null' and MetaPost will get out of its loop.
